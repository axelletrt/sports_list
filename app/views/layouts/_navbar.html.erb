		<div>
			<% if professional_signed_in? %>
		      <% if current_professional.avatar.attached? %>
		        <%= image_tag current_professional.avatar, height: 60, width: 60, class: "rounded-circle"  %>
		     <% end %> 
		    <% end %> 

	    	<% if user_signed_in? %>
	       	 <% if current_user.avatar_user.attached? %>
	           <%= image_tag current_user.avatar_user, height: 60, width: 60, class: "rounded-circle"  %> 
	 		 <% end %> 
	 		<% end %> 
	 	</div>


<ul>
  <% if user_signed_in? %>
   		<li> <%= link_to "Sign Out", destroy_user_session_path, method: :delete %> </li>
   		<li> <%= current_user.email %> </li>
   		<li> <%= link_to "Edit Account", edit_user_registration_path %> </li>
 <% else %>
   		<li> <%= link_to "sign in (user)", new_user_session_path %> </li>
    	<li> <%= link_to "sign up (user)", new_user_registration_path %> </li>
 <% end %>
</ul>
