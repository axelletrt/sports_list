

<% if current_user.professional.present?  %>







<div class="section1cp">
  <p class="titresectioncp">VOS FICHE</p>
  <div class="section1bg">
  <table>  
  <tr>
    <th><label class="container">

</label></th>
    <th>Activity Title</th>
    <th>Short Description</th>
    <th>City</th>
    <th>Modifier</th>
    <th>Supprimer</th>

   
      </th>
  </tr>
  <% @cards.each do |card| %>
  <tr>
    <td><label class="container">

</label></td>
    <td><%= card.activity_title %></td>
    <td><%= card.short_description %></td>
    <td><%= card.city %></td>
    <td><a href="<%= edit_card_path(card.id)%>" class= "btn btn-default">Modifier cette fiche</a></td>
    <td><%= link_to "Delete the card", card_path(card), method: :delete %> <br></td>

  
    <td></td>
  </tr>
  <% end %>
    <% end %>
 
</table>
  </div>
  </div>
  